/**
 * Application entrypoint package.
 *
 * This package contains the classes that start and coordinate the application
 * in both console (text) and graphical (Swing) modes. It includes the console
 * Main entry point and any GUI launcher classes that initialize the controller,
 * select the appropriate view (ConsoleView or SwingView) and manage the overall
 * application lifecycle for each mode.
 *
 * Use the console entry point for terminal/CLI usage and the GUI entry point
 * to run the Swing-based user interface.
 *
 * @author Kamil Fulneczek
 * @version 1.1
 */
package com.mycompany.app;